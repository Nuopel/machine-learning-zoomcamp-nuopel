# LOT-003: Model Training & Selection

**Duration:** 3 days | **Target:** 2025-11-17 | **Depends on:** LOT-002 ✅

## Objective

Train 3 regression models (Linear, Random Forest, XGBoost), compare performance, select best, save artifact, export train.py script.

---

## What You'll Deliver

- [ ] Notebook modeling section: 3 trained models with comparison table
- [ ] `src/train.py` — Training pipeline (reproducible)
- [ ] `models/best_model.pkl` — Saved model artifact
- [ ] Model comparison table (RMSE, MAE, R² on train/test)

---

## Acceptance Criteria

### 1. Data Preparation
- [ ] Train/test split: 80/20 with random seed set (for reproducibility)
- [ ] Feature preprocessing: categorical encoding, numerical scaling
- [ ] No data leakage (target not used in features)

### 2. Model 1: Linear Regression (Baseline)
- [ ] Trained with cross-validation (5-fold)
- [ ] Scores recorded: RMSE, MAE, R² on train & test
- [ ] Baseline established (expected RMSE ~€8-12)

### 3. Model 2: Random Forest
- [ ] Trained with tuned hyperparameters (e.g., n_estimators, max_depth)
- [ ] Cross-validated (5-fold)
- [ ] Scores recorded: RMSE, MAE, R² on train & test
- [ ] Should improve over baseline

### 4. Model 3: XGBoost
- [ ] Trained with tuned hyperparameters (e.g., max_depth, learning_rate)
- [ ] Cross-validated (5-fold)
- [ ] Scores recorded: RMSE, MAE, R² on train & test
- [ ] Best model likely to be XGBoost

### 5. Model Comparison
- [ ] Comparison table created (all scores side-by-side)
- [ ] Best model identified and justified
- [ ] Best model artifact saved: `models/best_model.pkl`

### 6. Reproducibility
- [ ] Random seed set (numpy, sklearn, xgboost)
- [ ] `src/train.py` script created
- [ ] train.py can be run standalone: `python src/train.py`
- [ ] Produces identical model when re-run with same data/seed

---

## Execution Steps

### Phase 1: Notebook Setup (20 minutes)

```python
# Cell 1: Imports & Setup
import pandas as pd
import numpy as np
from sklearn.model_selection import train_test_split, cross_val_score
from sklearn.linear_model import LinearRegression
from sklearn.ensemble import RandomForestRegressor
from sklearn.preprocessing import StandardScaler, LabelEncoder
from sklearn.metrics import mean_squared_error, mean_absolute_error, r2_score
import xgboost as xgb
import joblib

# Set random seeds for reproducibility
np.random.seed(42)

# Load data
df = pd.read_csv('data/wine_data_cleaned.csv')
print(f"Data shape: {df.shape}")
```

### Phase 2: Data Preparation (20 minutes)

```python
# Cell 2: Prepare features and target
X = df[['vintage_year', 'rating', 'region', 'grape_variety', 'food_pairings_count']].copy()
y = df['price'].copy()

# Encode categorical features
le_region = LabelEncoder()
le_grape = LabelEncoder()

X['region'] = le_region.fit_transform(X['region'])
X['grape_variety'] = le_grape.fit_transform(X['grape_variety'])

# Scale features
scaler = StandardScaler()
X_scaled = scaler.fit_transform(X)
X_scaled = pd.DataFrame(X_scaled, columns=X.columns)

# Split data
X_train, X_test, y_train, y_test = train_test_split(
    X_scaled, y, test_size=0.2, random_state=42
)

print(f"Train shape: {X_train.shape}")
print(f"Test shape: {X_test.shape}")
```

### Phase 3: Model 1 - Linear Regression (15 minutes)

```python
# Cell 3: Linear Regression
lr = LinearRegression()
lr.fit(X_train, y_train)

y_pred_train = lr.predict(X_train)
y_pred_test = lr.predict(X_test)

rmse_train = np.sqrt(mean_squared_error(y_train, y_pred_train))
rmse_test = np.sqrt(mean_squared_error(y_test, y_pred_test))
mae_test = mean_absolute_error(y_test, y_pred_test)
r2_test = r2_score(y_test, y_pred_test)

print("Linear Regression Results:")
print(f"  Train RMSE: {rmse_train:.2f}")
print(f"  Test RMSE:  {rmse_test:.2f}")
print(f"  Test MAE:   {mae_test:.2f}")
print(f"  Test R²:    {r2_test:.4f}")

lr_results = {
    'Model': 'Linear Regression',
    'Train RMSE': rmse_train,
    'Test RMSE': rmse_test,
    'Test MAE': mae_test,
    'Test R²': r2_test
}
```

### Phase 4: Model 2 - Random Forest (30 minutes)

```python
# Cell 4: Random Forest (tuned)
rf = RandomForestRegressor(
    n_estimators=100,
    max_depth=15,
    min_samples_split=5,
    random_state=42,
    n_jobs=-1
)
rf.fit(X_train, y_train)

y_pred_train = rf.predict(X_train)
y_pred_test = rf.predict(X_test)

rmse_train = np.sqrt(mean_squared_error(y_train, y_pred_train))
rmse_test = np.sqrt(mean_squared_error(y_test, y_pred_test))
mae_test = mean_absolute_error(y_test, y_pred_test)
r2_test = r2_score(y_test, y_pred_test)

print("Random Forest Results:")
print(f"  Train RMSE: {rmse_train:.2f}")
print(f"  Test RMSE:  {rmse_test:.2f}")
print(f"  Test MAE:   {mae_test:.2f}")
print(f"  Test R²:    {r2_test:.4f}")

rf_results = {
    'Model': 'Random Forest',
    'Train RMSE': rmse_train,
    'Test RMSE': rmse_test,
    'Test MAE': mae_test,
    'Test R²': r2_test
}

# Feature importance
import matplotlib.pyplot as plt
feature_importance = pd.DataFrame({
    'feature': X_scaled.columns,
    'importance': rf.feature_importances_
}).sort_values('importance', ascending=False)

print("\nFeature Importance (Random Forest):")
print(feature_importance)

fig, ax = plt.subplots(figsize=(8, 4))
feature_importance.plot(x='feature', y='importance', kind='barh', ax=ax)
ax.set_xlabel('Importance')
ax.set_title('Random Forest Feature Importance')
plt.tight_layout()
plt.show()
```

### Phase 5: Model 3 - XGBoost (30 minutes)

```python
# Cell 5: XGBoost (tuned)
xgb_model = xgb.XGBRegressor(
    n_estimators=100,
    max_depth=5,
    learning_rate=0.1,
    subsample=0.8,
    colsample_bytree=0.8,
    random_state=42,
    n_jobs=-1
)
xgb_model.fit(X_train, y_train, eval_set=[(X_test, y_test)], verbose=0)

y_pred_train = xgb_model.predict(X_train)
y_pred_test = xgb_model.predict(X_test)

rmse_train = np.sqrt(mean_squared_error(y_train, y_pred_train))
rmse_test = np.sqrt(mean_squared_error(y_test, y_pred_test))
mae_test = mean_absolute_error(y_test, y_pred_test)
r2_test = r2_score(y_test, y_pred_test)

print("XGBoost Results:")
print(f"  Train RMSE: {rmse_train:.2f}")
print(f"  Test RMSE:  {rmse_test:.2f}")
print(f"  Test MAE:   {mae_test:.2f}")
print(f"  Test R²:    {r2_test:.4f}")

xgb_results = {
    'Model': 'XGBoost',
    'Train RMSE': rmse_train,
    'Test RMSE': rmse_test,
    'Test MAE': mae_test,
    'Test R²': r2_test
}
```

### Phase 6: Model Comparison (10 minutes)

```python
# Cell 6: Compare all models
comparison_df = pd.DataFrame([lr_results, rf_results, xgb_results])
print("\n" + "="*60)
print("MODEL COMPARISON")
print("="*60)
print(comparison_df.to_string(index=False))

best_model_name = comparison_df.loc[comparison_df['Test RMSE'].idxmin(), 'Model']
print(f"\n🏆 Best Model: {best_model_name} (lowest Test RMSE)")

# Save comparison table
comparison_df.to_csv('models/model_comparison.csv', index=False)
```

### Phase 7: Save Best Model (10 minutes)

```python
# Cell 7: Save best model artifact
if best_model_name == 'XGBoost':
    best_model = xgb_model
elif best_model_name == 'Random Forest':
    best_model = rf
else:
    best_model = lr

joblib.dump(best_model, 'models/best_model.pkl')
joblib.dump(scaler, 'models/scaler.pkl')  # Save scaler too!

print(f"✅ Saved: models/best_model.pkl ({best_model_name})")
print(f"✅ Saved: models/scaler.pkl")
```

### Phase 8: Create train.py Script (30 minutes)

```python
# File: src/train.py
"""Training script for wine price prediction model."""

import pandas as pd
import numpy as np
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LinearRegression
from sklearn.ensemble import RandomForestRegressor
from sklearn.preprocessing import StandardScaler, LabelEncoder
from sklearn.metrics import mean_squared_error, mean_absolute_error, r2_score
import xgboost as xgb
import joblib

# Set random seeds
np.random.seed(42)

# Load data
df = pd.read_csv('data/wine_data_cleaned.csv')

# Prepare features
X = df[['vintage_year', 'rating', 'region', 'grape_variety', 'food_pairings_count']].copy()
y = df['price'].copy()

# Encode
le_region = LabelEncoder()
le_grape = LabelEncoder()
X['region'] = le_region.fit_transform(X['region'])
X['grape_variety'] = le_grape.fit_transform(X['grape_variety'])

# Scale
scaler = StandardScaler()
X_scaled = scaler.fit_transform(X)
X_scaled = pd.DataFrame(X_scaled, columns=X.columns)

# Split
X_train, X_test, y_train, y_test = train_test_split(
    X_scaled, y, test_size=0.2, random_state=42
)

# Train XGBoost (best model)
model = xgb.XGBRegressor(
    n_estimators=100,
    max_depth=5,
    learning_rate=0.1,
    subsample=0.8,
    colsample_bytree=0.8,
    random_state=42,
    n_jobs=-1
)
model.fit(X_train, y_train)

# Evaluate
y_pred_test = model.predict(X_test)
rmse = np.sqrt(mean_squared_error(y_test, y_pred_test))
mae = mean_absolute_error(y_test, y_pred_test)
r2 = r2_score(y_test, y_pred_test)

print(f"Test RMSE: {rmse:.2f}")
print(f"Test MAE:  {mae:.2f}")
print(f"Test R²:   {r2:.4f}")

# Save
joblib.dump(model, 'models/best_model.pkl')
joblib.dump(scaler, 'models/scaler.pkl')

print("✅ Model saved")
```

---

## Quality Gates

- [ ] All 3 models trained and scored
- [ ] Cross-validation used (5-fold)
- [ ] train.py reproduces model training
- [ ] Best model artifact saved

---

## Completion Checklist

- [ ] Read this LOT-003.md
- [ ] Execute all phases above
- [ ] Comparison table created
- [ ] Best model identified
- [ ] `src/train.py` created and tested
- [ ] `models/best_model.pkl` saved
- [ ] `git commit -m "Complete LOT-003: Model training and selection"`
- [ ] Update `wbs/STATUS.md` → LOT-003 complete
- [ ] Ready for LOT-004 (API)

---

## Notes

- Use GridSearchCV only if time permits; manual tuning OK
- Save both model AND scaler (needed in prediction)
- Document model selection rationale in notebook markdown
- If RMSE > €5, document findings and proceed anyway

---

**Target Completion:** 2025-11-17
**Next LOT:** LOT-004 (Web Service & Packaging)
